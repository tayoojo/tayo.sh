---
title: Hello World
description: The inaugural address.
date: 2024-10-06
published: true
---

# Hello World

This is `tayo.sh`

## This is a callout component

<Callout> We can do different types. This is default</Callout>

<Callout type="warning"> This is warning</Callout>

<Callout type="danger"> This is danger</Callout>

## We can create cool code blocks

```js {4} showLineNumbers
import { useFloating } from "@floating-ui/react";

function MyComponent() {
  const { refs, floatingStyles } = useFloating();

  return (
    <>
      <div ref={refs.setReference} />
      <div ref={refs.setFloating} style={floatingStyles} />
    </>
  );
}
```